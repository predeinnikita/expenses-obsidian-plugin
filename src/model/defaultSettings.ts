import type { ExpensesSettings } from "./ExpensesSettings";

export const DEFAULT_SETTINGS: ExpensesSettings = {
  expenses: [],
  incomes: [],
  monthsToShow: 6,
  baseCurrency: "RUB",
  language: "en",
};
