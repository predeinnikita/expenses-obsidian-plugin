{
  "name": "obsidian-expenses",
  "version": "0.0.1",
  "description": "Obsidian plugin to track expenses with monthly totals and currency conversion.",
  "main": "dist/main.js",
  "scripts": {
    "build": "node esbuild.config.mjs",
    "dev": "node esbuild.config.mjs --watch",
    "check": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.9.4",
    "esbuild": "^0.24.0",
    "obsidian": "^1.5.12",
    "typescript": "^5.6.3"
  },
  "dependencies": {
    "echarts": "^6.0.0"
  }
}
